{
  "scripts": {
    "dev": "snowpack dev",
    "build": "snowpack build",
    "deploy": "npm run build && node ./scripts/deploy.js",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@marlonmarcello/snowpack-plugin-pug": "^1.0.3",
    "@snowpack/plugin-sass": "^1.2.0",
    "gh-pages": "^3.1.0",
    "gsap": "npm:@gsap/shockingly@^3.5.1",
    "rollup": "^2.35.1",
    "snowpack": "^2.18.4",
    "snowpack-plugin-rollup-bundle": "^0.3.3"
  },
  "dependencies": {}
}
